# Cloudinary (client-safe values only — cloud name + an UNSIGNED upload preset).
# Never put API secrets here. If unset, VisualFlow stores generated assets locally.
VITE_CLOUDINARY_CLOUD_NAME=
VITE_CLOUDINARY_UPLOAD_PRESET=
